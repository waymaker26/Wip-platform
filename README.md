# WIP V0.2 Working

Upload `index.html`, `vercel.json`, and `README.md` to the root of the `wip-platform` GitHub repository.

Vercel settings:
- Framework preset: Other
- Build command: blank
- Output directory: blank

Functions included: Command Center, property list builder, add property, property details, deal analyzer, pipeline stage editing, tasks, CSV export, mobile layout, and browser persistence.
